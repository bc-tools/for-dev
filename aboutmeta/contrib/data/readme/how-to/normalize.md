### Normalize the standard version

The special method `normalize` must return a data-type object containing a **normalized** version of the `std` attribute, **without modifying any other attributes**.
An example usage of this method can be found in the `url.URL` class.
