#### `regex` patterns

The `regex` patterns can be either for a non recursive search as in `regex: "in-.*\\.md"`, or for a recursive one as in `r-regex: ".*/deep-.*\\.md"` with mandatory escaped backslahes.
